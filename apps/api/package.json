{
  "name": "@smart-pump/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "dev": "bun run --hot src/index.ts",
    "lint": "ultracite check",
    "format": "ultracite fix",
    "test": "vitest run",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.5",
    "@smart-pump/contracts": "workspace:*",
    "hono": "^4.12.18",
    "lowdb": "^7.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "^4.0.16"
  }
}
