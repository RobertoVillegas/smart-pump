# SMART Pump API

Hono API for the SMART Pump developer exercise.

## Scripts

Run from the repo root:

```sh
bun run dev --filter=@smart-pump/api
bun run typecheck --filter=@smart-pump/api
bun run build --filter=@smart-pump/api
```

The local API server runs on `http://localhost:3001`.
